<?php

    ///// This is just for testing the anti-spam feature

    // ini_set('display_errors', 1);
    // ini_set('display_startup_errors', 1);
    // error_reporting(E_ALL);

    // require_once 'db.php';

    // $db = new DB();

    // echo 'IP: ' . $_SERVER['REMOTE_ADDR'] . PHP_EOL;
    // echo 'This action is ' . ($db->antiSpam($_SERVER['REMOTE_ADDR']) ? 'allowed!' : 'not allowed!');

?>